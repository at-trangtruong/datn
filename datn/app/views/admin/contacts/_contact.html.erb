<tr>
<td class="align-center"><%= contact_counter + 1 %></td>
  <td class="align-center"><%= contact.name %></td>
  <td class="align-center"><%= contact.email %></td>
  <td class="align-center"><%= contact.phone %></td>
  <td class="align-center"><%= contact.content %></td>
  <td align="center">
    <%= link_to "Xóa", admin_contact_path(contact.id) , method: :delete, data: {confirm: "Are you sure ?"} %>
  </td>
</tr>
